<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="@dimen/margin_all_item"
    android:layout_marginBottom="@dimen/margin_all_item">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_weekly_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Dimanche 6" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_weekly_item_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryDark"
        app:layout_constraintStart_toStartOf="@+id/tv_weekly_item_title"
        app:layout_constraintTop_toBottomOf="@+id/tv_weekly_item_title"
        tools:text="Une trés belle journée" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_weekly_item_icon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:adjustViewBounds="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_clear_day" />

</androidx.constraintlayout.widget.ConstraintLayout>